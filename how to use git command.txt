touch .git ignore
git config --global user.name Dron
git config --global user .email "dronr19@gmail.com"
git  init
git add.

git commit -m "Initial commit "
git status #just to see (working tree clean)
ssh-keygen -t ed25519 -C "dronr19@gmail.com"
tail<paith to id_rsa.pub> #add thois key to your git hub account

git remote add origin <ssh url.git>
git push origin master

